#include <iostream>
using namespace std;

int main(){

    int a = 4, b = 6;

    // Bitwise Operation
    cout<<"AND & Operation: "<<(a & b)<<endl;
    cout<<"OR | Operation: "<<(a | b)<<endl;
    cout<<"Not ~ Operation: "<<(~a)<<endl;
    cout<<"XOR ^ Operation: "<<(a ^ b);

    return 0;
}